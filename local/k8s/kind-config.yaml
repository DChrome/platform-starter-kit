kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

# Canonical local cluster for Platform Starter Kit.
# Cluster name is set by the wrapper script (kind.sh) to avoid duplicating it here.

nodes:
  - role: control-plane
  - role: worker
